<Window x:Class="ClipboardPaste.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClipboardPaste"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        

        <Button x:Name="btnPaste" Grid.ColumnSpan="2" Click="ButtonPaste_Click">Paste</Button>
        <TextBlock HorizontalAlignment="Center" Grid.Column="2" Margin="5,0,0,0">Content</TextBlock>
        <TextBlock VerticalAlignment="Center" Grid.Row="1" Text="Content types" >
            <TextBlock.LayoutTransform>
                <RotateTransform  Angle="-90" />
            </TextBlock.LayoutTransform>
        </TextBlock>

        <ListBox Grid.Column="1" Grid.Row="1" x:Name="lstContentType"/>
        <ListBox Grid.Column="2" Grid.Row="1" x:Name="lstContent"/>
        
        
        
    </Grid>
</Window>
